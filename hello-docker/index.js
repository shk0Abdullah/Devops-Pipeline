console.log("Assalam u alaikum Docker")


// docker build .
// docker run <containerName>
// docker run -it hello-docker sh
// You can use the shell to change the files while docker container runtime 
// but they will only change for that runtime.